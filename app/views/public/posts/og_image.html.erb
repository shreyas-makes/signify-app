<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=1200, height=630, initial-scale=1">
    <title><%= @post.title %> - Signify</title>
    <style>
      :root {
        color-scheme: only light;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        width: 1200px;
        height: 630px;
        font-family: "Georgia", "Times New Roman", serif;
        background: #0b0b0b;
        color: #f5f5f5;
      }

      .frame {
        position: relative;
        height: 100%;
        padding: 64px 72px;
        background:
          radial-gradient(circle at 12% 20%, rgba(255, 255, 255, 0.08), transparent 48%),
          radial-gradient(circle at 85% 15%, rgba(255, 255, 255, 0.06), transparent 45%),
          linear-gradient(135deg, #0b0b0b, #131313);
        border: 1px solid rgba(255, 255, 255, 0.12);
      }

      .title {
        margin: 22px 0 0;
        font-size: 64px;
        line-height: 1.05;
        font-weight: 600;
        max-width: 980px;
      }

      .subtitle {
        margin-top: 18px;
        font-size: 26px;
        line-height: 1.35;
        color: #d7d7d7;
        max-width: 960px;
      }

      .meta {
        position: absolute;
        bottom: 56px;
        left: 72px;
        right: 72px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-family: "Helvetica Neue", "Arial", sans-serif;
      }

      .author {
        font-size: 22px;
        font-weight: 600;
        color: #f5f5f5;
      }

      .date {
        font-size: 18px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #c8c8c8;
      }

      .logo {
        font-size: 20px;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: #d7d7d7;
      }
    </style>
  </head>
  <body>
    <div class="frame">
      <div class="title"><%= @post.title %></div>
      <% if @post.subtitle.present? %>
        <div class="subtitle"><%= @post.subtitle %></div>
      <% elsif @excerpt.present? %>
        <div class="subtitle"><%= @excerpt %></div>
      <% end %>
      <div class="meta">
        <div>
          <div class="author"><%= @post.user.display_name %></div>
          <div class="date"><%= @published_date %></div>
        </div>
        <div class="logo">signifywriting.com</div>
      </div>
    </div>
  </body>
</html>
